const db = require('./database');

function uploadImage({ name, data }){
    // get the file name

    // convert file into binary?

    // save file into database

    return { status: 501, body: "" }
}

module.exports = { uploadImage };